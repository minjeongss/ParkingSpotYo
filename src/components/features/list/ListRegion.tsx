import styled from '@emotion/styled'

const Text = styled.p({
  color: '#666666',
  fontWeight: 'bold',
  fontSize: '20px',
})
const ListRegion = () => {
  return <Text>송파구 근처 주차장이에요.</Text>
}

export default ListRegion
